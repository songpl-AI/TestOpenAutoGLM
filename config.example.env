# Open-AutoGLM 环境变量配置模板
# 复制此文件为 .env 并填入实际值

# =============================================================================
# API 提供商配置
# =============================================================================

# API 提供商类型: local（本地/自建） 或 zhipu（智谱AI）
# 如果设置了 ZHIPU_API_KEY，会自动使用智谱 AI
PHONE_AGENT_PROVIDER=local

# =============================================================================
# 智谱 AI 配置（方案 C - 云端 API）
# =============================================================================

# 智谱 AI 的 API Key（从 https://open.bigmodel.cn 获取）
# ZHIPU_API_KEY=your-zhipu-api-key-here

# 智谱 AI 使用的模型名称
# 推荐: glm-4v-plus（最新视觉模型）
# PHONE_AGENT_MODEL=glm-4v-plus

# =============================================================================
# 本地/自建服务器配置（方案 A/B）
# =============================================================================

# 本地 API 服务地址（vLLM/SGLang 部署的服务）
PHONE_AGENT_BASE_URL=http://localhost:8000/v1

# 本地模型名称
# PHONE_AGENT_MODEL=autoglm-phone-9b

# API 密钥（本地部署通常不需要，保持 EMPTY 即可）
# PHONE_AGENT_API_KEY=EMPTY

# =============================================================================
# Agent 配置
# =============================================================================

# 每个任务的最大执行步数
PHONE_AGENT_MAX_STEPS=100

# 语言设置: cn（中文） 或 en（英文）
PHONE_AGENT_LANG=cn

# ADB 设备 ID（留空则自动检测第一个设备）
# PHONE_AGENT_DEVICE_ID=

# =============================================================================
# 高级配置
# =============================================================================

# 模型参数
# PHONE_AGENT_MAX_TOKENS=3000
# PHONE_AGENT_TEMPERATURE=0.1
# PHONE_AGENT_TOP_P=0.85
# PHONE_AGENT_FREQUENCY_PENALTY=0.2

# 网络配置（针对云端 API）
# PHONE_AGENT_TIMEOUT=120
# PHONE_AGENT_MAX_RETRIES=3

# =============================================================================
# 使用示例
# =============================================================================

# 示例 1: 使用智谱 AI（推荐新手）
# -----------------------------------
# ZHIPU_API_KEY=your-api-key-here
# PHONE_AGENT_MODEL=glm-4v-plus

# 示例 2: 使用本地部署
# -----------------------------------
# PHONE_AGENT_PROVIDER=local
# PHONE_AGENT_BASE_URL=http://localhost:8000/v1
# PHONE_AGENT_MODEL=autoglm-phone-9b

# 示例 3: 使用远程服务器
# -----------------------------------
# PHONE_AGENT_PROVIDER=local
# PHONE_AGENT_BASE_URL=http://your-server-ip:8000/v1
# PHONE_AGENT_API_KEY=your-server-api-key
# PHONE_AGENT_MODEL=autoglm-phone-9b

# =============================================================================
# 使用说明
# =============================================================================

# 1. 复制此文件为 .env
#    cp config.example.env .env

# 2. 编辑 .env 文件，填入您的配置
#    vim .env  # 或使用其他编辑器

# 3. 运行项目（会自动加载 .env 文件）
#    python main.py "打开微信"

# 注意：
# - .env 文件不应该提交到 Git（已在 .gitignore 中）
# - 不要在代码中硬编码敏感信息（如 API Key）
# - 生产环境建议使用密钥管理服务

